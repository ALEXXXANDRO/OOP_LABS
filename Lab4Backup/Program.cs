using System;

namespace Lab4Backup
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Backup backup1 = new Backup();
            backup1.AddFiles("D:\\ИНСТРУМЕНТЫ\\adiosity\\Audacity\\audacity.exe");
            for (int i = 0; i < 10; i++)
            {
                BackupManager.manager.SplitCopyАlgorithm(backup1, false,
                    "D:\\ИНСТРУМЕНТЫ\\adiosity\\Audacity\\audacity.exe");
            }
            
            Console.WriteLine(BackupManager.manager.BackupList[0].RestorePointsList.Count);
            
            Cleaner<Algorithms> cleaner = new Cleaner<Algorithms>();
            
            AmountRemoveAlgorithm alg1 = new AmountRemoveAlgorithm(backup1, 8);
            AmountRemoveAlgorithm alg2 = new AmountRemoveAlgorithm(backup1, 4);
            cleaner.addAlgoritm(alg1);
            cleaner.addAlgoritm(alg2);

            cleaner.RemovePoint(backup1,cleaner.GetExtraPoint(false));
            Console.WriteLine(BackupManager.manager.BackupList[0].RestorePointsList.Count);
            */
        }
    }
}